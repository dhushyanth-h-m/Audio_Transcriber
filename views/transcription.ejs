<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Transcription Result</title>
  <link rel="stylesheet" href="https://generative-ui.com/cdn/generative.css">
  <style>
    .transcription-container {
      max-width: 600px;
      margin: 0 auto;
      padding: 2rem;
      background-color: #f5f5f5;
      border-radius: 0.5rem;
      box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
    }

    .transcription-text {
      white-space: pre-wrap;
      line-height: 1.6;
      text-align: justify;
    }
  </style>
</head>
<body>
  <div class="transcription-container">
    <h1 class="text-2xl font-bold mb-4">Transcription Result</h1>
    <div id="transcription">
      <% if (transcription) { %>
        <p class="transcription-text"><%= transcription %></p>
      <% } else { %>
        <p class="text-gray-500">No transcription available.</p>
      <% } %>
    </div>
  </div>
</body>
</html>